name: Deploy Production

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch, tag, or SHA to deploy"
        required: true
        default: main

concurrency:
  group: ilovelawyer-app-deploy-production
  cancel-in-progress: true

env:
  ECR_REPOSITORY: ilovelawyer-app
  AWS_REGION: ap-southeast-1
  IMAGE_TAG: ${{ github.sha }}

jobs:
  deploy:
    name: Lint, Build, Push & Deploy (production)
    runs-on: ubuntu-latest
    environment: production

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.ref || github.sha }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Get ECR auth token
        id: ecr-token
        run: |
          echo "password=$(aws ecr get-login-password --region ${{ env.AWS_REGION }})" >> $GITHUB_OUTPUT

      # NEXT_PUBLIC_* values are inlined into the client JS bundle at `next build` time,
      # not read at container runtime — see docs/adr/0005-web-env-vars-baked-at-build-time.md.
      # This file must exist before `docker build` runs, since the builder stage's
      # `COPY . .` is what carries it into the image.
      - name: Write web env file
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}
        run: |
          cat > ${{ github.workspace }}/apps/web/.env.staging << EOF
          NODE_ENV=${NODE_ENV}
          NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
          NEXT_PUBLIC_GOOGLE_CLIENT_ID=${NEXT_PUBLIC_GOOGLE_CLIENT_ID}
          EOF

      # EC2 is Graviton (linux/arm64). ubuntu-latest builds amd64 by default,
      # which produces: exec /usr/local/bin/docker-entrypoint.sh: exec format error
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/arm64
          provenance: false
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:latest

      - name: Clean up stale env path on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            [ -d /home/ec2-user/ilovelawyer-app/.env.staging ] && rm -rf /home/ec2-user/ilovelawyer-app/.env.staging || true

      - name: Copy env file to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "apps/web/.env.staging"
          target: "/home/ec2-user/ilovelawyer-app"
          strip_components: 2

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        env:
          ECR_PASSWORD: ${{ steps.ecr-token.outputs.password }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          DEPLOY_IMAGE_TAG: ${{ env.IMAGE_TAG }}
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          command_timeout: 20m
          envs: ECR_PASSWORD,ECR_REGISTRY,DEPLOY_IMAGE_TAG
          script: |
            set -e
            echo "$ECR_PASSWORD" | docker login --username AWS --password-stdin "$ECR_REGISTRY"
            docker pull --platform linux/arm64 "$ECR_REGISTRY/ilovelawyer-app:$DEPLOY_IMAGE_TAG"

            docker stop ilovelawyer-app 2>/dev/null || true
            docker rm   ilovelawyer-app 2>/dev/null || true
            docker run -d --name ilovelawyer-app --restart unless-stopped \
              --platform linux/arm64 \
              -p 3002:3002 \
              --env-file /home/ec2-user/ilovelawyer-app/.env.staging \
              "$ECR_REGISTRY/ilovelawyer-app:$DEPLOY_IMAGE_TAG"
            docker image prune -f
